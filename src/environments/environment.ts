import 'zone.js/plugins/zone-error';
import { IEnvironment } from '@environments/environment.model';

// Deliberately leaving this file (including the API Key) outside the .gitignore since this is merely a sample app.
export const environment: IEnvironment = {
  production: false,
  unsplashAccessKey: 'Y1LXLBFD7SwqcqMPCFNcwXUZxj7RqAxvj09MIL8Rpsg'
};
